/* miniprogram/pages/station-ranking/station-ranking.wxss */

/* ========== 全局容器 ========== */
.container {
  width: 100%;  /* 🔧 新增：确保100%宽度 */
  box-sizing: border-box;  /* 🔧 新增：padding包含在宽度内 */
  min-height: 100vh;
  background: linear-gradient(180deg, #f5f7fa 0%, #e9ecef 100%);
  padding: 20rpx;
  padding-bottom: 40rpx;
}

/* ========== 模式切换标签 ========== */
.mode-tabs {
  display: flex;
  background: #fff;
  border-radius: 20rpx;
  padding: 10rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.mode-tab {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx 20rpx;
  border-radius: 16rpx;
  transition: all 0.3s;
  cursor: pointer;
}

.mode-tab.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 6rpx 16rpx rgba(102, 126, 234, 0.3);
}

.tab-icon {
  font-size: 40rpx;
  margin-right: 12rpx;
}

.tab-text {
  font-size: 30rpx;
  font-weight: 600;
  color: #666;
}

.mode-tab.active .tab-text {
  color: #fff;
}

/* ========== 排名类型选择区域 - 优化为网格布局 ========== */
.ranking-types {
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.section-title-inline {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.title-icon {
  font-size: 36rpx;
  margin-right: 12rpx;
}

.title-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

/* 网格布局 - 避免横向滚动和截断 */
.type-grid {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.type-row {
  display: flex;
  gap: 20rpx;
  justify-content: space-between;
}

.type-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 120rpx;
  padding: 20rpx 10rpx;
  background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
  border-radius: 16rpx;
  border: 2rpx solid transparent;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.type-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 0;
}

.type-item.active::before {
  opacity: 1;
}

.type-item.active {
  border-color: #667eea;
  transform: translateY(-4rpx);
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.25);
}

.type-icon {
  font-size: 44rpx;
  margin-bottom: 10rpx;
  position: relative;
  z-index: 1;
  filter: grayscale(0.5);
  transition: all 0.3s ease;
}

.type-item.active .type-icon {
  filter: grayscale(0);
  transform: scale(1.1);
}

.type-name {
  font-size: 26rpx;
  font-weight: 600;
  color: #666;
  white-space: nowrap;
  position: relative;
  z-index: 1;
  transition: color 0.3s ease;
}

.type-item.active .type-name {
  color: #fff;
}

/* ========== 排名榜单区域 ========== */
.ranking-section {
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.section-title {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.title-subtitle {
  margin-left: auto;
  font-size: 24rpx;
  color: #999;
}

/* ========== 排名列表项 ========== */
.ranking-list {
  margin-top: 20rpx;
}

.ranking-item {
  position: relative;
  display: flex;
  align-items: center;
  padding: 28rpx 24rpx;
  margin-bottom: 20rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  border-radius: 20rpx;
  border: 2rpx solid #f0f0f0;
  transition: all 0.3s ease;
  overflow: hidden;
}

.ranking-item:active {
  transform: scale(0.98);
}

.ranking-item:hover {
  border-color: #667eea;
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.15);
}

/* 排名徽章 */
.rank-badge {
  width: 88rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 50%;
  font-size: 40rpx;
  color: #fff;
  font-weight: bold;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
}

.rank-badge.top-three {
  background: linear-gradient(135deg, #ffd700 0%, #ffed4e 100%);
  box-shadow: 0 4rpx 16rpx rgba(255, 215, 0, 0.4);
}

.rank-text {
  font-size: 44rpx;
}

.rank-number {
  font-size: 36rpx;
}

/* 站点信息 */
.station-info {
  flex: 1;
  min-width: 0;
  margin-right: 20rpx;
}

.station-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.station-id {
  font-size: 24rpx;
  color: #999;
}

/* 指标值 */
.metric-value {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
  min-width: 120rpx;
}

.value-number {
  font-size: 40rpx;
  font-weight: bold;
  color: #667eea;
  line-height: 1;
}

.value-number.positive {
  color: #52c41a;
}

.value-number.negative {
  color: #f5222d;
}

.value-unit {
  font-size: 22rpx;
  color: #999;
  margin-top: 6rpx;
}

/* 进度条 */
.progress-bar {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: rgba(0, 0, 0, 0.05);
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
  transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 切换按钮 */
.toggle-list-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 30rpx;
  padding: 24rpx;
  background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
  border-radius: 16rpx;
  color: #667eea;
  font-size: 28rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
}

.toggle-list-btn:active {
  transform: scale(0.98);
}

.arrow {
  margin-left: 12rpx;
  font-size: 24rpx;
  transition: transform 0.3s;
}

.arrow.up {
  transform: rotate(180deg);
}

/* ========== 对比模式 ========== */
.compare-section {
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.selected-stations {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  margin-top: 20rpx;
}

.selected-station {
  display: flex;
  align-items: center;
  padding: 20rpx 28rpx;
  border-radius: 50rpx;
  font-size: 28rpx;
  font-weight: 500;
  color: #fff;
  transition: all 0.3s;
}

.selected-station:active {
  transform: scale(0.95);
}

/* 为不同站点设置不同颜色 */
.station-color-0 {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.station-color-1 {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

.station-color-2 {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

.station-color-3 {
  background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
}

.station-color-4 {
  background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
}

.remove-btn {
  margin-left: 16rpx;
  font-size: 44rpx;
  font-weight: bold;
  opacity: 0.8;
  transition: opacity 0.3s;
}

.remove-btn:active {
  opacity: 1;
}

.add-station-btn {
  display: flex;
  align-items: center;
  padding: 20rpx 28rpx;
  background: #fff;
  border: 3rpx dashed #d0d0d0;
  border-radius: 50rpx;
  color: #999;
  font-size: 28rpx;
  transition: all 0.3s;
}

.add-station-btn:active {
  border-color: #667eea;
  color: #667eea;
  transform: scale(0.95);
}

.add-icon {
  margin-right: 8rpx;
  font-size: 36rpx;
}

.compare-hint {
  display: flex;
  align-items: center;
  margin-top: 24rpx;
  padding: 20rpx;
  background: #fff9e6;
  border-radius: 12rpx;
  border-left: 4rpx solid #faad14;
}

.hint-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.hint-text {
  font-size: 26rpx;
  color: #d48806;
}

/* ========== 图表区域 ========== */
/* 🔧 关键修复：强制设置宽度为100% */
.chart-section {
  width: 100% !important;  /* 🔧 新增：强制宽度100% */
  box-sizing: border-box;  /* 🔧 新增：确保padding包含在宽度内 */
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 0rpx;  /* 🔧 修改：左右padding从 10rpx 改为 0rpx */
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

/* 给section-title单独设置padding */
.chart-section .section-title {
  padding-left: 24rpx;
  padding-right: 24rpx;
}

.chart-container {
  width: 100% !important;  /* 🔧 强化：确保100%宽度 */
  box-sizing: border-box;  /* 🔧 新增：确保padding包含在宽度内 */
  height: 850rpx;  /* 🔧 修改：从750rpx增加到850rpx，给雷达图更多垂直空间 */
  margin-top: 20rpx;
  position: relative;
  background: #fafafa;
  border-radius: 16rpx;
  overflow: hidden;
}

ec-canvas {
  width: 100% !important;  /* 🔧 添加 !important 确保优先级 */
  height: 100% !important;  /* 🔧 添加 !important 确保优先级 */
  box-sizing: border-box;  /* 🔧 新增：确保padding包含在宽度内 */
  display: block;
}

/* 🔧 新增：强制ec-canvas的canvas元素也使用100%宽度 */
ec-canvas canvas {
  width: 100% !important;
  height: 100% !important;
}

/* ========== 对比表格 ========== */
.compare-table-section {
  width: 100%;  /* 🔧 新增：固定为100%宽度 */
  box-sizing: border-box;
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx 0rpx;  /* 🔧 修改：左右padding改为0，让表格占满宽度 */
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

/* 给section-title单独设置padding */
.compare-table-section .section-title {
  padding-left: 24rpx;
  padding-right: 24rpx;
}

.table-scroll {
  width: 100%;
  margin-top: 20rpx;
  overflow-x: hidden;  /* 🔧 修改：禁用横向滚动 */
  overflow-y: visible;
}

.compare-table {
  width: 100%;  /* 🔧 修改：固定为100%宽度 */
  display: flex;  /* 🔧 修改：使用flex布局 */
  flex-direction: column;
}

.table-header,
.table-body {
  display: flex;
  flex-direction: column;
  width: 100%;
}

.table-row {
  display: flex;
  width: 100%;  /* 🔧 新增：每行占满100%宽度 */
  border-bottom: 1rpx solid #f0f0f0;
}

.table-row:last-child {
  border-bottom: none;
}

.table-cell {
  flex: 1;  /* 🔧 修改：改回flex:1，让所有数据列平均分配剩余空间 */
  min-width: 0;  /* 🔧 修改：允许收缩 */
  padding: 24rpx 8rpx;  /* 🔧 修改：减小左右padding从16rpx到8rpx */
  text-align: center;
  font-size: 24rpx;  /* 🔧 修改：从26rpx减小到24rpx，节省空间 */
  color: #333;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  box-sizing: border-box;
}

.header-cell {
  background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
  font-weight: 600;
  font-size: 24rpx;  /* 🔧 修改：从28rpx减小到24rpx */
  padding: 24rpx 8rpx;  /* 🔧 修改：减小左右padding */
  border-right: 1rpx solid #fff;
  box-sizing: border-box;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.header-cell:first-child {
  border-top-left-radius: 12rpx;
  flex: 0 0 140rpx;  /* 🔧 新增：第一列（指标列）固定宽度 */
  min-width: 140rpx;
}

.header-cell:last-child {
  border-top-right-radius: 12rpx;
  border-right: none;
}

/* 🔧 新增：非第一列的表头单元格，平均分配剩余空间 */
.header-cell:not(:first-child) {
  flex: 1;
  min-width: 0;
}

.station-header {
  color: #fff;  /* 保持白色 */
  font-weight: 600;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.2);  /* 🔧 新增：添加文字阴影提高可读性 */
}

/* 🔧 新增：为每个站点颜色单独设置更深的背景 */
.station-header.station-color-0 {
  background: linear-gradient(135deg, #5568d3 0%, #6741a5 100%);  /* 更深的紫色 */
}

.station-header.station-color-1 {
  background: linear-gradient(135deg, #e07ce0 0%, #d93f5a 100%);  /* 更深的粉色 */
}

.station-header.station-color-2 {
  background: linear-gradient(135deg, #3d8ee6 0%, #00d4e6 100%);  /* 更深的蓝色 */
}

.station-header.station-color-3 {
  background: linear-gradient(135deg, #2fd466 0%, #1fdfbd 100%);  /* 更深的绿色 */
}

.station-header.station-color-4 {
  background: linear-gradient(135deg, #e65a89 0%, #f0c830 100%);  /* 更深的橙黄色 */
}

.label-cell {
  background: #fafafa;
  font-weight: 500;
  color: #666;
  flex: 0 0 140rpx;  /* 🔧 修改：固定宽度140rpx，不伸缩 */
  min-width: 140rpx;  /* 🔧 修改：减小到140rpx */
  padding: 24rpx 8rpx;  /* 🔧 修改：减小左右padding */
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.label-icon {
  font-size: 24rpx;  /* 🔧 修改：从28rpx减小到24rpx */
  margin-right: 6rpx;  /* 🔧 修改：从8rpx减小到6rpx */
}

.label-text {
  font-size: 24rpx;  /* 🔧 修改：从26rpx减小到24rpx */
}

.data-cell {
  font-size: 24rpx;  /* 🔧 修改：从28rpx减小到24rpx */
  font-weight: 500;
  color: #333;
  padding: 24rpx 8rpx;  /* 🔧 新增：确保padding一致 */
  box-sizing: border-box;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.positive {
  color: #52c41a;
  font-weight: 600;
}

.negative {
  color: #f5222d;
  font-weight: 600;
}

/* ========== 站点选择弹窗 ========== */
.station-picker-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9999;  /* 🔧 修改：从1000增加到9999 */
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);  /* 🔧 修改：增加不透明度从0.5到0.6 */
  backdrop-filter: blur(4rpx);
  z-index: 1;  /* 🔧 新增：确保遮罩在底层 */
}

.modal-content {
  position: relative;
  width: 85%;
  max-width: 600rpx;
  max-height: 80vh;
  background: #fff;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.2);
  z-index: 2;  /* 🔧 新增：确保内容在遮罩上层 */
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx 28rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
}

.modal-close {
  font-size: 48rpx;
  font-weight: 300;
  opacity: 0.8;
  cursor: pointer;
  transition: opacity 0.3s;
}

.modal-close:active {
  opacity: 1;
}

.station-list {
  max-height: 60vh;
  padding: 16rpx 0;
}

.station-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 28rpx;
  margin: 0 16rpx 12rpx;
  background: #fafafa;
  border-radius: 16rpx;
  transition: all 0.3s;
}

.station-item:active {
  transform: scale(0.98);
}

.station-item.selected {
  background: linear-gradient(135deg, #e8ebff 0%, #f0e6ff 100%);
  border: 2rpx solid #667eea;
}

.station-item-content {
  flex: 1;
}

.station-item .station-name {
  font-size: 30rpx;
  font-weight: 500;
  color: #333;
  margin-bottom: 8rpx;
}

.station-item .station-id {
  font-size: 24rpx;
  color: #999;
}

.check-icon {
  font-size: 36rpx;
  color: #667eea;
  font-weight: bold;
}